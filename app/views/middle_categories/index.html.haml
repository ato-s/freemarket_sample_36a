%h1 Listing middle_categories

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @middle_categories.each do |middle_category|
      %tr
        %td= link_to 'Show', middle_category
        %td= link_to 'Edit', edit_middle_category_path(middle_category)
        %td= link_to 'Destroy', middle_category, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Middle category', new_middle_category_path
